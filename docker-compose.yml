version: "3.8"

services:
  db:
    image: mariadb:10
    ports:
      - "3306:3306"
    volumes:
      - "tbb_mariadb_data:/var/lib/mysql"
    environment:
      - MYSQL_ROOT_PASSWORD=frb
      - MYSQL_DATABASE=frb
      - MYSQL_USER=frb
      - MYSQL_PASSWORD=frb

volumes:
  tbb_mariadb_data:
